# 文件夹同步助手 - FreeSync 使用说明

## 1. 软件简介

**文件夹同步助手 - FreeSync** 是一款用于 Windows 的文件夹同步工具，适合在两个文件夹之间复制、合并、同步删除文件，并支持实时同步文件变化。

它主要用于：

- 本地两个目录之间同步文件；
- 移动硬盘、U 盘、NAS 映射盘之间同步资料；
- 工作目录和备份目录之间保持一致；
- 两台设备共享目录之间进行双向合并；
- 自动监控目录变化并实时同步。

---

## 2. 主界面功能介绍

### 2.1 同步任务列表

主界面上方的列表用于显示当前已添加的同步任务。

每一条同步任务包含：

- 源文件夹；
- 目标文件夹；
- 同步方向。

显示方式：

- `源文件夹 -> 目标文件夹`：表示单向同步；
- `源文件夹 <-> 目标文件夹`：表示双向同步。

---

### 2.2 添加同步任务

点击主界面的 `➕` 按钮，可以打开“添加同步任务”窗口。

在该窗口中可以设置：

1. **源文件夹**  
   要同步出去的文件夹。

2. **目标文件夹**  
   接收同步内容的文件夹。

3. **双向同步**  
   勾选后，两个文件夹会互相同步。

#### 支持拖入文件夹

“添加同步任务”窗口支持直接拖入文件夹：

- 拖入 1 个文件夹：自动填入源文件夹或目标文件夹；
- 拖入 2 个文件夹：第一个作为源文件夹，第二个作为目标文件夹；
- 拖入文件时，会自动识别该文件所在的文件夹路径；
- 拖到“源文件夹”输入框，会填入源路径；
- 拖到“目标文件夹”输入框，会填入目标路径。

#### 使用场景

适合快速添加同步目录，例如：

- 把 `E:\工作资料` 同步到 `F:\备份\工作资料`；
- 把 U 盘目录同步到电脑目录；
- 把两个项目资料目录设置为双向同步。

---

### 2.3 删除同步任务

点击主界面的 `➖` 按钮，可以删除当前选中的同步任务。

使用步骤：

1. 在同步任务列表中选中一条任务；
2. 点击 `➖`；
3. 该任务会从列表和配置文件中移除。

#### 使用场景

当某个同步关系不再需要时，可以删除该任务，避免后续误同步。

---

## 3. 同步模式介绍

### 3.1 单向同步

单向同步的方向为：

```text
源文件夹 -> 目标文件夹
```

同步时，软件会把源文件夹中的文件复制到目标文件夹中。

如果目标文件夹中已经存在同名文件，软件会比较文件大小和修改时间，只在需要时才复制，避免重复覆盖。

#### 适用场景

- 将工作目录备份到移动硬盘；
- 将电脑资料同步到 NAS；
- 将主目录内容复制到备份目录；
- 保持目标目录和源目录一致。

---

### 3.2 双向同步

双向同步的方向为：

```text
源文件夹 <-> 目标文件夹
```

双向同步会合并两个文件夹的内容：

- A 文件夹新增的文件会同步到 B 文件夹；
- B 文件夹新增的文件会同步到 A 文件夹；
- A 文件夹修改的文件会同步到 B 文件夹；
- B 文件夹修改的文件会同步到 A 文件夹；
- 一侧删除的文件，会同步删除另一侧对应文件。

软件使用本地快照记录上一次同步状态，用于判断文件是“新增”“修改”还是“删除”。

#### 适用场景

- 两个工作目录都可能产生修改；
- 公司电脑和家里电脑通过移动硬盘同步；
- 本地目录和云盘目录之间互相合并；
- 多个位置都可能编辑同一批资料。

---

## 4. 同步删除文件

主界面中的 `同步删除文件` 选项用于控制是否删除目标中多余的文件。

### 4.1 单向同步下的作用

如果勾选 `同步删除文件`：

- 源文件夹中不存在的文件；
- 但目标文件夹中存在；

这些目标文件会被删除。

也就是说，目标文件夹会尽量保持和源文件夹一致。

如果不勾选：

- 只会复制或更新文件；
- 不会删除目标文件夹中的多余文件。

#### 使用场景

适合用于镜像备份：

```text
源文件夹内容 = 目标文件夹内容
```

例如：

- `D:\照片` 完整同步到 `E:\照片备份`；
- 删除源目录中过期文件后，备份目录也自动删除对应文件。

---

### 4.2 双向同步下的删除逻辑

双向同步会根据历史快照判断删除行为。

例如：

1. 上一次同步时，A 和 B 都有 `test.txt`；
2. 用户在 A 中删除 `test.txt`；
3. 下次同步时，软件会识别出这是 A 发起的删除；
4. 软件会同步删除 B 中的 `test.txt`。

反过来，如果用户在 B 中删除文件，也会同步删除 A 中对应文件。

#### 使用场景

适合两边都需要维护同一批文件的情况，例如：

- 两台电脑之间同步文档；
- 移动硬盘和本地目录之间双向维护；
- 删除无用文件时希望另一侧也自动清理。

---

## 5. 空文件夹清理

当文件被同步删除后，如果某些子文件夹已经没有任何内容，软件会自动清理这些空文件夹。

例如：

```text
A:\资料\项目1\说明.txt
B:\资料\项目1\说明.txt
```

如果删除 `说明.txt` 后，`项目1` 文件夹为空，同步后空文件夹也会被删除。

#### 使用场景

适合保持目录结构干净，避免删除文件后残留大量空目录。

---

## 6. 实时同步

主界面中的 `实时同步` 用于监听文件夹变化并自动同步。

勾选后，软件会自动监控同步任务中的文件夹。

当检测到文件变化时，会自动触发同步。

### 6.1 实时同步会监听哪些变化

包括：

- 新增文件；
- 修改文件；
- 删除文件；
- 新增文件夹；
- 删除文件夹；
- 文件属性变化；
- 文件修改时间变化。

### 6.2 实时同步配置保存

`实时同步` 的勾选状态会写入配置文件。

如果上次关闭软件时开启了实时同步，下次启动软件时会自动恢复该状态。

如果没有同步任务，软件会自动取消实时同步。

#### 使用场景

适合需要自动同步的场景：

- 工作时自动备份文件；
- 修改文件后自动同步到移动硬盘；
- 文件夹变化后自动同步到另一目录；
- 不想每次手动点击同步。

---

## 7. 手动同步

点击 `手动同步` 按钮，可以立即执行所有同步任务。

手动同步会按照当前任务列表逐项处理。

同步过程中：

- 进度条会显示同步进度；
- 日志窗口会显示复制、删除、失败等信息；
- 同步完成后会弹出提示。

#### 使用场景

- 不想开启实时同步，只在需要时同步；
- 插入移动硬盘后手动同步；
- 检查同步任务配置是否正确；
- 批量同步多个任务。

---

## 8. 日志窗口

日志窗口用于显示软件运行过程中的信息。

常见日志包括：

- 配置文件位置；
- 已加载同步任务数量；
- 同步开始；
- 文件复制；
- 文件删除；
- 空目录删除；
- 同步完成；
- 实时同步开启或关闭；
- 操作失败原因。

#### 使用场景

当同步结果不符合预期时，可以查看日志判断原因。

例如：

- 文件是否被复制；
- 文件是否被删除；
- 是否因为权限问题失败；
- 是否目标路径不存在；
- 是否实时同步触发了同步。

---

## 9. 进度条

进度条显示当前同步过程的大致进度。

在同步大量文件时，可以通过进度条了解任务处理情况。

---

## 10. 系统托盘

软件关闭窗口时，会提示：

- 选择“是”：最小化到系统托盘；
- 选择“否”：退出程序；
- 选择“取消”：不关闭窗口。

托盘图标支持：

- 双击显示主窗口；
- 右键打开菜单；
- 从菜单中显示窗口或退出程序。

#### 使用场景

适合开启实时同步后，让软件在后台运行。

---

## 11. 配置文件说明

软件配置文件保存在：

```text
%APPDATA%\FreeSync\settings.ini
```

配置内容包括：

- 是否开启同步删除文件；
- 是否默认开启实时同步；
- 同步任务数量；
- 每个任务的源文件夹；
- 每个任务的目标文件夹；
- 每个任务是否为双向同步。

---

## 12. 快照文件说明

双向同步会使用快照文件记录上一次同步状态。

快照文件保存在：

```text
%APPDATA%\FreeSync\*.snapshot
```

快照记录内容包括：

- 文件相对路径；
- 文件大小；
- 文件最后修改时间。

快照用于判断：

- 文件是新增；
- 文件是修改；
- 文件是删除。

一般情况下不需要手动修改快照文件。

删除双向同步任务时，软件会自动删除该任务对应的快照文件，避免旧任务状态长期残留。

如果出现历史状态异常，可以关闭软件后删除对应快照文件，再重新执行一次同步，让软件重新建立快照。

---

## 13. 推荐使用方式

### 13.1 只做备份

推荐设置：

- 使用单向同步；
- 勾选 `同步删除文件`；
- 根据需要开启实时同步。

适合：

- 工作目录备份；
- 照片备份；
- 文档备份。

---

### 13.2 两边都可能修改

推荐设置：

- 使用双向同步；
- 保持两边文件夹都可读写；
- 第一次同步前确认两个目录内容没有明显冲突。

适合：

- 两台电脑之间同步；
- 本地目录和移动硬盘之间同步；
- 多位置维护同一批资料。

---

### 13.3 长期后台自动同步

推荐设置：

- 添加好同步任务；
- 勾选 `实时同步`；
- 关闭窗口时选择最小化到托盘。

适合：

- 工作过程中自动备份；
- 持续监控文件夹变化；
- 避免忘记手动同步。

---

## 14. 注意事项

1. **首次双向同步前建议先备份重要文件。**  
   双向同步会合并、覆盖或删除文件，建议先确认目录内容。

2. **同步删除文件请谨慎使用。**  
   勾选后，一侧删除可能导致另一侧同步删除。

3. **不要同时让多个同步工具操作同一批目录。**  
   这可能造成重复触发、冲突或文件状态混乱。

4. **移动硬盘或网络盘断开时，请先停止实时同步。**  
   避免路径不可用导致同步失败。

5. **如果文件正在被其他程序占用，可能同步失败。**  
   可关闭占用程序后重新同步。

---

## 15. 常见问题

### Q1：为什么删除一侧文件后，另一侧也被删除？

这是双向同步和同步删除逻辑的预期行为。软件会根据快照判断用户删除了文件，并同步删除另一侧对应文件。

### Q2：为什么目标目录中多余文件被删除？

因为勾选了 `同步删除文件`。如果不希望删除目标目录中的多余文件，请取消勾选。

### Q3：为什么启动后自动开启实时同步？

因为上次关闭软件前勾选了 `实时同步`，该状态已经保存到配置文件中。

### Q4：为什么拖入文件也能识别路径？

添加同步任务窗口会自动把拖入文件转换为它所在的文件夹路径。

### Q5：日志太多会不会无限增长？

不会。日志窗口会限制最大长度，超过后会自动清理较早的日志内容。

---

## 16. 总结

文件夹同步助手 - FreeSync 适合需要简单、快速、自动同步文件夹的用户。

它支持：

- 单向同步；
- 双向同步；
- 同步删除文件；
- 空目录清理；
- 实时同步；
- 托盘后台运行；


对于日常备份、资料同步、移动硬盘同步、工作目录自动同步等场景都比较适用。
