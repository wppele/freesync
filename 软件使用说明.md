# 文件夹同步助手 FreeSync 使用说明

## 软件简介

**FreeSync** 是一款 Windows 文件夹同步工具，用于在两个文件夹之间同步文件、文件夹和删除操作。

它适合用于：

- 本地文件夹备份；
- 电脑与移动硬盘、U 盘之间同步资料；
- 两个工作目录之间保持一致；
- 两台设备之间通过共享目录或移动硬盘同步文件；
- 后台实时监听文件变化并自动同步。

---

## 运行环境

- **操作系统**：Windows 10 (版本 1703 或更高) / Windows 11。
- **架构**：支持 x86 (32位) 和 x64 (64位)。
- **提示**：本软件使用了现代 C++17 技术，**不支持** Windows XP、Windows 7 或 Windows 8.1。

---

## 主要功能

### 1. 单向同步

单向同步的方向是：

```text
源文件夹 -> 目标文件夹
```

软件会把源文件夹中的内容同步到目标文件夹。

适合场景：

- 工作资料备份到移动硬盘；
- 照片、文档备份到另一个目录；
- 将电脑目录同步到 NAS 或共享盘；
- 让备份目录跟随主目录更新。

如果勾选 **同步删除文件**，源文件夹中已经删除的内容，也会从目标文件夹中删除，使目标目录尽量和源目录一致。

---

### 2. 双向同步

双向同步的方向是：

```text
源文件夹 <-> 目标文件夹
```

两个文件夹会互相同步：

- 一边新增的文件或文件夹，会同步到另一边；
- 一边修改的文件，会同步到另一边；
- 一边删除的文件或文件夹，也会同步删除另一边对应内容；
- 空文件夹也会像普通文件一样参与同步。

适合场景：

- 公司电脑和家里电脑之间同步工作资料；
- 本地目录和移动硬盘之间双向维护；
- 两个位置都会修改同一批文件；
- 希望新增、修改、删除都保持一致。

首次使用双向同步前，建议先确认两个目录中的内容，避免不期望的覆盖或删除。

---

### 3. 实时同步

勾选 **实时同步** 后，软件会监听同步任务中的文件夹变化。

当检测到新增、修改、删除文件或文件夹时，会自动执行同步。

适合场景：

- 工作时自动备份；
- 文件一变化就同步到移动硬盘；
- 不想每次手动点击同步；
- 软件最小化到托盘后长期后台运行。

---

### 4. 移动硬盘和 U 盘盘符变化处理

添加同步任务时，软件会记录磁盘标识和文件夹在磁盘中的位置。

如果 U 盘或移动硬盘重新插入后盘符变化，例如：

```text
E:\资料  ->  F:\资料
```

软件会在启动、手动同步或开启实时同步前尝试自动恢复路径。

如果磁盘没有插入，或磁盘被重新格式化导致标识变化，则需要插入正确磁盘或重新添加任务。

---

## 基本使用方式

1. 点击 `➕` 添加同步任务。
2. 选择源文件夹和目标文件夹。
3. 根据需要选择是否启用 **双向同步**。
4. 根据需要选择是否勾选 **同步删除文件**。
5. 点击 **手动同步** 立即同步，或勾选 **实时同步** 自动同步。
6. 关闭窗口时可选择最小化到系统托盘，让软件继续后台运行。

添加任务窗口支持拖入文件夹，也可以把文件拖进去，软件会自动识别该文件所在的文件夹。

---

## 推荐使用场景

### 只做备份

推荐：

- 使用单向同步；
- 根据需要勾选 **同步删除文件**；
- 需要自动备份时开启 **实时同步**。

适合工作资料、照片、文档等备份。

### 两边都可能修改

推荐：

- 使用双向同步；
- 第一次同步前检查两边目录；
- 重要资料建议先备份一次。

适合电脑之间、电脑与移动硬盘之间同步同一批资料。

### 长期后台自动同步

推荐：

- 添加好同步任务；
- 勾选 **实时同步**；
- 关闭窗口时选择最小化到托盘。

适合持续自动备份和长期文件夹监控。

---

## 注意事项

- 双向同步和同步删除文件会同步删除操作，请谨慎使用。
- 文件正在被其他程序占用时，可能会同步失败，关闭占用程序后可重新同步。
- 移动硬盘或 U 盘未插入时，对应任务无法正常同步。
- 不建议多个同步工具同时操作同一批目录。

---

## 总结

FreeSync 主要用于简单、快速地同步两个文件夹。

它支持单向同步、双向同步、同步删除、空文件夹同步、实时同步、托盘后台运行，以及移动硬盘盘符变化自动恢复。
